% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{ingrain_profile}
\alias{ingrain_profile}
\title{Partition of occurrence records across a sweep of analysis grains}
\usage{
ingrain_profile(
  occ,
  grains = 10^seq(1, 5, length.out = 61),
  mark = NULL,
  uncertainty = "coordinateUncertaintyInMeters"
)
}
\arguments{
\item{occ}{A data.frame of occurrence records, e.g. a GBIF download.}

\item{grains}{Numeric vector of at least two candidate grains (raster
cell edge lengths, in metres). Defaults to 61 log-spaced values from
10 m to 100 km.}

\item{mark}{Optional single grain (metres) to highlight: it is added
to the sweep and \code{\link[=autoplot]{autoplot()}} draws a reference line there, with the
exact shares at that grain in the subtitle.}

\item{uncertainty}{Name of the column holding the positional
uncertainty radius in metres.}
}
\value{
A data.frame with columns \code{grain}, \code{state}, \code{n} and \code{share},
of class \code{"ingrain_profile"}, with the record count and any \code{mark}
stored as attributes.
}
\description{
Computes the four-state partition of \code{\link[=ingrain]{ingrain()}} at every grain in a
sweep, showing how the split between \code{inert}, \code{marginal}, \code{actionable}
and \code{unreported} records shifts with the resolution of the intended
analysis. The \code{unreported} share is constant across the sweep: it is
the portion of the data that no choice of resolution can touch.
}
\examples{
p <- ingrain_profile(crayfish, mark = 1000)
p
autoplot(p)
}
\seealso{
\code{\link[=ingrain]{ingrain()}} for the audit at a single grain.
}
