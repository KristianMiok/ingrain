% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{ingrain_u}
\alias{ingrain_u}
\title{How much does the dataset determine the state of a record?}
\usage{
ingrain_u(object, dataset = "datasetKey", B = 200, seed = NULL, min_n = 1L)
}
\arguments{
\item{object}{An \code{ingrain_audit} object from \code{\link[=ingrain]{ingrain()}}.}

\item{dataset}{Name of the column identifying the publishing dataset.}

\item{B}{Number of permutations for the null. \code{B = 0} skips the null
and returns only \code{u}.}

\item{seed}{Optional integer seed for the permutation null; the
caller's random-number state is restored afterwards.}

\item{min_n}{Drop datasets with fewer than \code{min_n} records (their
records are excluded from the computation entirely).}
}
\value{
An object of class \code{"ingrain_u"}: a list with elements \code{u},
\code{null} (vector of length \code{B}), \code{u_null} (null mean), \code{sd_null},
\code{excess}, \code{p} (permutation p-value), \code{B}, \code{n_records},
\code{n_datasets}.
}
\description{
Computes the uncertainty coefficient (Theil's U) of the four-state
partition given the publishing dataset,
\deqn{U = 1 - H(state | dataset) / H(state),}
with plug-in entropies in bits: \code{U = 0} means the dataset tells you
nothing about a record's state, \code{U = 1} that it determines it
completely. Because a plug-in U is inflated by having many groups, it
is compared against a permutation null in which state labels are
reshuffled across records, holding dataset sizes and the overall
state counts fixed; the quantity to interpret is the excess of U over
the null mean. With states collapsed to two classes this estimator
and its null reduce exactly to the binary uncertainty-coefficient
construction used in the analyses behind the package; there, the two
classes were \code{actionable} versus all other states pooled, computed
over datasets with at least 500 records.
}
\examples{
a <- ingrain(crayfish, grain = 1000)
ingrain_u(a, B = 50, seed = 1)
}
