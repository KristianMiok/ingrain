% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{by_dataset}
\alias{by_dataset}
\title{Partition of an ingrain audit by publishing dataset}
\usage{
by_dataset(object, dataset = "datasetKey", min_n = 1L)
}
\arguments{
\item{object}{An \code{ingrain_audit} object from \code{\link[=ingrain]{ingrain()}}.}

\item{dataset}{Name of the column identifying the publishing dataset.
Defaults to the Darwin Core term \code{"datasetKey"}.}

\item{min_n}{Drop datasets with fewer than \code{min_n} records. Defaults
to 1 (keep all); shares of very small datasets are unstable.}
}
\value{
A data.frame of class \code{"ingrain_datasets"}, sorted by record
count, with columns \code{dataset}, \code{n}, \code{inert}, \code{marginal},
\code{actionable}, \code{unreported} and \code{default_radius}. The grain, the
total record and dataset counts and \code{min_n} are stored as
attributes.
}
\description{
Aggregates the four-state partition of \code{\link[=ingrain]{ingrain()}} per dataset,
returning one row per dataset with its record count and the share of
records in each state. The column \code{default_radius} reports the share
of records whose radius is one of the four GBIF-documented geocoder
default values (301, 999, 3036, 9999 m); these records keep their
ordinary state in the partition, and the column exists as a
diagnostic.
}
\examples{
a <- ingrain(crayfish, grain = 1000)
by_dataset(a)
}
\seealso{
\code{\link[=ingrain_u]{ingrain_u()}} for how much the dataset determines the state.
}
